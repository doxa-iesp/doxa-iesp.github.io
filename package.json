{
  "name": "doxa-site",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Site do DOXA — Laboratório de Estudos Eleitorais, de Comunicação Política e Opinião Pública (IESP-UERJ)",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "validar": "node scripts/validar-dados.mjs",
    "build": "npm run validar && astro build",
    "preview": "astro preview",
    "check": "npm run validar && astro check"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.6.0",
    "@fontsource-variable/montserrat": "^5.2.5",
    "astro": "^7.0.7",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^6.0.3"
  }
}
